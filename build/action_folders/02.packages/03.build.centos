# build Packages

declare -a OPTS=( '--virtual' '.buildDependencies' ) 

declare -a PKGS=(build-base
                 expat
                 expat-devel
                 file
                 gcc-c++
                 gcc-objc
                 git
                 gnutls-utils
                 glibc
                 glibc-devel
                 libtool
                 make )
