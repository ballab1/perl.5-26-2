# build Packages

#declare -a OPTS=( '.buildDependencies' ) 

declare -a PKGS=( expat-devel
                  file
                  gcc-objc
                  git
                  gnutls-utils
                  libtool )
