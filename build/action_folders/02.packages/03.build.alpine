# build Packages

declare -a OPTS=( '--virtual' '.buildDependencies' )
declare -a PKGS=( build-base
                  expat
                  expat-dev
                  file
                  g++
                  gcc
                  gcc-objc
                  git
                  gnutls-utils
                  libc6-compat
                  libtool
                  make )
